version: '3.8'

services:
  fesvelte:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: svelte-frontend
    ports:
      - "3001:80"
    restart: unless-stopped
    # 如果需要连接到后端API，可以配置网络
    # networks:
    #   - app-network

# networks:
#   app-network:
#     driver: bridge


