version: '3.8'

services:
  chat-backend:
    build: .
    container_name: smlrag-chat-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

